[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ecoroute_core"
version = "0.1.0"
description = "Rust core for EcoRoute"
requires-python = ">=3.8"
dependencies = []
